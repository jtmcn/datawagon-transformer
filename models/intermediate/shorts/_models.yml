version: 2

models:
  - name: int__shorts__channel__ad_rev_monthly
    description: ""
    columns:
      - name: surrogate_key
        tests:
          - unique:
              config:
                where: report_date = '2024-03-31'
          - not_null:
              config:
                where: report_date = '2024-03-31'


  - name: int__shorts__channel__sub_rev_daily
    description: ""
    columns:
      - name: surrogate_key
        tests:
          - unique:
              config:
                where: report_date = '2024-03-31'
          - not_null:
              config:
                where: report_date = '2024-03-31'

